<% data = format_for_chart(@platforms, 10)%>
<center><h4>
  Most used <b>research platforms</b>:
  <%= raw data.first.take(3).map { |a| "<code>#{ a[:chart_description] }</code>" }.to_sentence %>
</h4></center>
<%= render partial: 'shared/stats_bar_graph',
           locals: { title: 'Platforms_bar_graph',
                     data: data,
                     height: 300,
                     unit: 'Percentage' } %>
<p>
  Fig. 2d: <b>Research platform use (top 10)</b><br>
</p>
