<% data = format_for_chart(@focusgroups, 10) %>
<center><h4>
  Most studied <b>focal taxa</b>:
  <%= raw data.first.take(3).map { |a| "<code>#{ a[:chart_description] }</code>" }.to_sentence %>
</h4></center>
<%= render partial: 'shared/stats_bar_graph',
           locals: { title: 'Focusgroups_bar_graph',
                     data: data,
                     height: 300,
                     unit: 'Percentage' } %>
<p>
  Fig. 2b: <b>Focal taxa (top 10)</b><br>
</p>
