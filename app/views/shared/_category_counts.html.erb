<% if not defined? exclusion or category.description != exclusion %>
  <span class="badge">
    <%= category.count %>
  </span>&nbsp;<%= link_to category.description, summary_path(category.class.name.downcase.pluralize, category) %>
  <br>
<% end %>
