<% if post.photos.count < 6 %>
  Coming soon...
<% else %>
  <table>
    <tr>
    <td>
    	<%= link_to image_tag(post.photos[3].image(:compo)),
                  post_pages_path(post) %>
    </td>
     <td>
      <%= link_to image_tag(post.photos[2].image(:compo)),
                  post_pages_path(post) %>
    </td>
   </tr>
   </table>
<% end %>
