<%= render partial: "layouts/page_title",
       locals: {title: "News",
                breadcrumbs: [["News", nil]] } %>

<%= paginate @posts %>
<% @posts.each do |post| %>
  <%= render partial: 'shared/post_summary',
             locals: {post: post } %>
<% end %>
<%= paginate @posts %>
