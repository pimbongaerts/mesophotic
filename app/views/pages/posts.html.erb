<%= render partial: "layouts/page_title",
       locals: {title: "Latest posts",
                backlink: root_path } %>

<%= paginate @posts %>
<% @posts.each do |post| %>
  <%= render partial: 'shared/post_summary',
             locals: {post: post } %>
<% end %>
<%= paginate @posts %>
