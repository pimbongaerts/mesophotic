<%= render partial: "layouts/page_title",
       	   locals: {title: "Species",
            		backlink: root_path } %>

<div class="row">
  <div class="col-sm-12">
    <% @focusgroups.each do |focusgroup| %>
      <%= render partial: 'specieslist', 
                 locals: { focusgroup: focusgroup, 
                           species_list: @species.where(focusgroup: focusgroup)} %>
    <% end %>
  </div>
</div>